<h2 class="mt-3">ショッピングカート</h2>
 <%= form_with model:@cart_item do |f| %>
  <h4>title</h4>
   <%= f.text_field :title %>
  <h4>Opinion</h4>
   <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
 <% end %>